{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "apps/backend/Dockerfile"
  },
  "deploy": {
    "startCommand": "cd /app && (NODE_OPTIONS='--import tsx' /app/node_modules/.bin/medusa db:migrate || echo 'migrate failed, continuing') && NODE_OPTIONS='--import tsx' /app/node_modules/.bin/medusa start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3,
    "numReplicas": 1,
    "healthcheckPath": "/health",
    "healthcheckTimeout": 60
  }
}
